{
  "name": "noteflow-backend",
  "version": "1.0.0",
  "description": "NoteFlow API server with PostgreSQL",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "db:test": "node -e \"require('dotenv').config(); require('./db').connectDB().then(()=>{console.log('PostgreSQL connected'); process.exit(0)}).catch(e=>{console.error(e.message); process.exit(1)})\""
  },
  "dependencies": {
    "@google/generative-ai": "^0.22.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "pg": "^8.13.3",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.5",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}